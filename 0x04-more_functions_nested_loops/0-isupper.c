#include "main.h"

/**
 * _isupper - function to check uppercase alphabets
 * @c: Int representing a character
 * Return: 0 or 1
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z');
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
